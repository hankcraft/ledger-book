{
  "name": "ledger-book-poc",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun --filter '@ledger-book/api' --filter '@ledger-book/web' --parallel dev",
    "build": "bun --filter '@ledger-book/contracts' typecheck && bun --filter '@ledger-book/api' build && bun --filter '@ledger-book/web' build",
    "test": "bun --filter '@ledger-book/api' test",
    "typecheck": "bun --filter '@ledger-book/contracts' typecheck && bun --filter '@ledger-book/api' typecheck && bun --filter '@ledger-book/web' typecheck",
    "lint": "oxlint apps packages",
    "lint:fix": "oxlint --fix apps packages",
    "format": "oxfmt apps packages",
    "format:check": "oxfmt --check apps packages",
    "prepare": "lefthook install"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "lefthook": "2.1.10",
    "oxfmt": "0.58.0",
    "oxlint": "1.73.0",
    "typescript": "5.9.3",
    "vue-tsc": "3.3.7"
  }
}
